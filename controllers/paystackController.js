const connection = require('../modules/db')

// var paystack = require('paystack')('secret_key')
// exports.payStackSubscribe = () => {
    
//     connection.query(``, (err, resp) => {
//         if(err) {return res.status(500).json({message: 'There has been an error, try again'})}
        
//         if(resp){

//         }
//     })
// } 